# Dependencias solo para ejecutar la batería de tests (no necesarias en producción)
pytest>=8.0
pytest-cov>=5.0    # informe de cobertura (py -m pytest --cov=. --cov-report=term-missing)
httpx>=0.27        # requerido por fastapi.testclient (Starlette TestClient)
