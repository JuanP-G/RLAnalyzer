[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: tests puros sin BD ni red
    api: tests de endpoints con BD temporal en memoria y mocks
